<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.github.persiliao</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>2026.1.1</version>
        <relativePath/> <!-- lookup parent from repository -->
    </parent>

    <artifactId>mqtt-spring-boot-starter</artifactId>

    <name>Mqtt Spring Boot Starter</name>
    <description>Feature-rich, enterprise-grade MQTT client Spring Boot Starter with auto-configuration</description>
    <url>https://github.com/persiliao/mqtt-spring-boot-starter</url>

    <licenses>
        <license>
            <name>MIT License</name>
            <url>https://opensource.org/licenses/MIT</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <developers>
        <developer>
            <id>persiliao</id>
            <name>Persi.Liao</name>
            <email>xiangchu.liao@gmail.com</email>
            <organization>persiliao</organization>
            <organizationUrl>https://github.com/persiliao</organizationUrl>
        </developer>
    </developers>

    <scm>
        <connection>scm:git:https://github.com/persiliao/mqtt-spring-boot-starter.git</connection>
        <developerConnection>scm:git:https://github.com/persiliao/mqtt-spring-boot-starter.git</developerConnection>
        <url>https://github.com/persiliao/mqtt-spring-boot-starter</url>
    </scm>

    <properties>
        <!--
          Compile to Java 17 bytecode (class file version 61); the jar requires
          a Java 17+ runtime. The Spring Boot version is NOT pinned here — it
          stays overridable via ${spring-boot.version} inherited from the parent,
          but the enforcer rule below rejects any release below 3.x, so this
          artifact serves Spring Boot 3.x+ only.
        -->
        <maven.compiler.release>17</maven.compiler.release>
        <hivemq.version>1.3.13</hivemq.version>
        <jetbrains.annotations.version>13.0</jetbrains.annotations.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-autoconfigure</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
            <optional>true</optional>
        </dependency>
        <!-- HiveMQ MQTT Client -->
        <dependency>
            <groupId>com.hivemq</groupId>
            <artifactId>hivemq-mqtt-client</artifactId>
            <version>${hivemq.version}</version>
        </dependency>
        <!-- For @Async support -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-aop</artifactId>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <scope>compile</scope>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.jetbrains</groupId>
            <artifactId>annotations</artifactId>
            <version>${jetbrains.annotations.version}</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!--
              Enforce the minimum supported Spring Boot version (3.x). The
              parent imports the Spring Boot BOM driven by the overridable
              ${spring-boot.version} property; this rule rejects any build
              that points it at a 2.x release.
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-enforcer-plugin</artifactId>
                <version>${maven-enforcer-plugin.version}</version>
                <executions>
                    <execution>
                        <id>enforce-minimum-spring-boot</id>
                        <goals>
                            <goal>enforce</goal>
                        </goals>
                        <configuration>
                            <rules>
                                <requireProperty>
                                    <property>spring-boot.version</property>
                                    <regex>[3-9](\.\d+)+(-[A-Za-z0-9.-]+)?</regex>
                                    <message>The minimum supported Spring Boot version is 3.x, but ${spring-boot.version} is configured.</message>
                                </requireProperty>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
